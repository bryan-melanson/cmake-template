#include "crc.h"

/* STM32H5 CRC driver implementation — replace with your own. */

void CRC_Init(void) { }

uint32_t CRC_Calculate(const uint8_t *data, unsigned int len)
{
    (void)data; (void)len;
    return 0;
}

uint32_t CRC_Accumulate(const uint8_t *data, unsigned int len)
{
    (void)data; (void)len;
    return 0;
}
