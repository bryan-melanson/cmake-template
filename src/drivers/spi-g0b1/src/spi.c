#include "spi.h"

/* STM32G0 SPI driver implementation — replace with your own. */

void SPI_Init(void) { }

void SPI_Transfer(unsigned int spi, const uint8_t *tx, uint8_t *rx, unsigned int len)
{
    (void)spi; (void)tx; (void)rx; (void)len;
}
